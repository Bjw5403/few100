

describe('writing specs in typescript', () => {
    it('is easy', () => {
        expect(true).toBe(true);
    });
    it('can add two number', () => {
        const a = 10, b = 20
        const answer = a + b

        expect(answer).toBe(30)
    })
});

//DESC
// IT